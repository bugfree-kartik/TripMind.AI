{
  "buildCommand": "cd ui && npx react-scripts build",
  "outputDirectory": "ui/build",
  "framework": null,
  "installCommand": "cd ui && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
